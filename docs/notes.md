# notes.md（已停用，內容已搬遷）

原本的 N1–N14 待辦清單已依 `docs/agent-guideline.md` §1 的規定欄位遷移至 `docs/PENDING.md`，
請改看該檔。舊記錄仍可用 `git log -p -- docs/notes.md` 或 `git show <commit>:docs/notes.md` 查閱。

開頭原有的「T1b–T3 計畫表」是 `changes/extract-per-group-staging`（revision 3）已結案的執行任務
清單，不是延後問題，未搬遷；其結果已收斂於 `changes/extract-per-group-staging/CHANGE_REPORT.md`。

之後新發現的延後問題請直接寫入 `docs/PENDING.md`，不要再用本檔。
