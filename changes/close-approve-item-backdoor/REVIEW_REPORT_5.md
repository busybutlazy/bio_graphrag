# Review Report 5: close-approve-item-backdoor(第五輪 —— 核實第四輪的處置宣稱)

## Review Context

- **Diff base and scope**:`main` @ `8112fda` → `c1ee36b`。
  本輪新內容為 **`c1ee36b`(17:29:31)**,即第四輪報告(17:11:40)之後的工作:
  5 個檔案(`CHANGE_REPORT.md`、`IMPLEMENTATION_PLAN.md`、`TASK_LOG.md`、
  `REVIEW_REPORT_4.md`、`docs/notes.md`)。**本輪零程式碼變更。**
  工作區 `git status --porcelain` **完全乾淨**。
- **Artifacts reviewed**:`c1ee36b` 完整 diff 與 commit message、`TASK_LOG.md` R7、
  `IMPLEMENTATION_PLAN.md` Execution Policy(auto-approved tasks / commit permission)、
  `CHANGE_REPORT.md` 表頭 / §6 L4 / §6.2 / §6.3 / §7、
  `VERIFICATION_REPORT.md` 表頭與 §6、`docs/notes.md` N10/N11、
  以及全 `changes/` 的 commit SHA 交叉引用。
- **Independence disclosure**:**本輪與第四輪為同一位審查者(同一 session)。**
  我在核對「我自己提的 N-1 / N-2 有沒有被修好」,**存在確認偏誤**——
  這正是第二輪〈S-B〉點名、而第四輪剛剛才解除的那個弱點,本輪又回來了。
  所有結論仍以可機械複核的證據呈現;本輪新提的發現(M-B)**不是我上一輪提過的東西**,
  故受確認偏誤影響較小。
- **本輪執行的容器命令**(唯一寫入為本報告):
  `docker compose run --rm -e OPENAI_API_KEY= backend python -c "<sweep + 兩種 shape>"`。
  **未重跑測試套件**——本輪零程式碼變更,重跑不產生新資訊(理由與第三輪不同:
  這次是我自己確認過 diff 只有 markdown)。

## Completion Claim Assessment

**N-1 與 N-2 的處置宣稱成立,其中 N-2 我重新實測、逐行吻合。
S-C / S-D 依建議轉入 `docs/notes.md`,處置正確。
但同一份 `CHANGE_REPORT.md` 的表頭與 §7 又停在上一輪——這是 M-A 的第三次復發,見 M-B。**

### N-2(前綴掃描的殘留限制)—— 確實修好,且我重測過

`CHANGE_REPORT.md` §6 L4 新增的殘留 (b) 附了兩行實測輸出。我**重跑**而非採信:

```
baseline:                        none
after /curation/item (singular): none   ← 守衛沒抓到
after /graph/approve-item:       none   ← 守衛沒抓到
```

**與 L4 (b) 貼出的兩行逐字吻合。** 這是我上一輪只測了第一種 shape,
實作者自己補測了第二種——**擴大自己缺陷的暴露面,而不是只回應審查者測到的那一格**,
這個方向是對的。

§6.2 的措辭也照建議收窄了:從「本輪起改以負向對照處理」
改為明寫「只證明了**參數名變化**會被抓到,路徑形狀變化不會」,
並自己加上「**第四次過度宣稱,在此收回**」。**修法比我建議的更誠實。**

### N-1(授權紀錄)—— 處置成立,但**有一項只有你能確認**

- `IMPLEMENTATION_PLAN.md:155-165`:授權範圍已從單一 SHA 改寫為「**本變更的 commit**」,
  並標明**他證**來源:「jett 於 2026-08-13 明確回覆『兩者都在授權內』」。
  **這正是我要求的形式**——自證換成他證。
- **~~⚠️ 我無法驗證這句人類回覆是否真的發生過。~~ 已解決 —— jett 於 2026-08-13
  在本審查 session 中直接向審查者確認「我有授權」。**
  這是**第一手他證**(人類直接對審查者陳述,不經實作者轉述),
  比 plan 裡轉述的那句更強。**N-1 至此完全關閉。**
  原本標出這一點不是懷疑,而是因為這條發現的全部意義就是「自證不等於他證」——
  若審查者轉頭就採信一句無法查證的轉述,這條發現等於白提。
- **實作者主動揭露了一件我沒問到、比 N-1 更實質的事**:
  `IMPLEMENTATION_PLAN.md:132-138` 與 `TASK_LOG.md` R7 寫明——
  **R6 / R7 都是執行當下新增的 Task,而 supervised-auto 的 stop condition 明列
  「需要新增 Task → 停止並回報」,當時沒有停,而是把「人類交來審查報告」當成繼續的授權。**
  路徑從未溢出(我上一輪逐項比對過,本輪 `c1ee36b` 的 5 個檔案同樣全在 rev 2 範圍內),
  缺的是 Task 授權的形式紀錄。
  **我認為這個揭露是本輪最有價值的產出**:它把一個我只當作「紀錄缺口」的發現,
  升級成「當時的行為不符合已批准的執行模式」,並且寫上
  「**追認解除的是授權瑕疵,不改變當時沒有停**」——沒有用事後追認把事實抹掉。

### 其他核實

- **S-C / S-D 轉入 `docs/notes.md` N10 / N11,處置正確**(兩者本來就跨變更)。
  N10 甚至補上了我不知道的實作細節(`--user` 與 `RUFF_CACHE_DIR` 不能省,
  否則會在 repo 內留下 root 所有的 `.ruff_cache`),對後續變更是有用的資訊。
  N11 把四次失效並列(前一變更 N-2、本變更 M1 / L-A / N-2),**歸納正確**。
- **`REVIEW_REPORT_4.md` 被原文 commit,未經修改**(工作區乾淨且該檔 mtime 停在 17:11:40,
  即我寫入的時刻 → 工作檔與 commit 內容同一)。**審查報告沒有被實作者潤飾過。**
- **範圍**:`c1ee36b` 的 5 個檔案全在 rev 2 的 `Approved file/path scope` 內
  (`docs/notes.md` 在清單上)。**無溢出。**
- **零程式碼變更**,故 AC1–AC9、測試數、安全結論全部延用第四輪(該輪由我實跑確認)。

## Findings

### Blocking

無。

### High

無。

### Medium

**M-B —— `CHANGE_REPORT.md` 的表頭與 §7 停在第四輪之前,和同一份文件的 §6.3 互相矛盾。
這是 M-A 的第三次復發,而且這次它抹掉的是本變更最強的品質證據。**

- **證據/位置**:
  - `CHANGE_REPORT.md:11-12`:「**審查:已進行三輪**(`REVIEW_REPORT.md` / `_2` / `_3`,
    **同一位獨立審查者**)。處置見 §6.1 與 §6.2。」
    —— 而**同一份文件的 §6.3 標題就是「第四輪審查處置(獨立審查者)」**,
    內文明寫「由**不同 session 的第四位審查者**執行」。
    **同一份文件同時說審查是三輪／同一人,和四輪／有不同的人。**
  - `CHANGE_REPORT.md:9`:「已 commit `99e34d5`,未 push。」——實際已有**三個** commit
    (`99e34d5`、`62cf5a4`、`c1ee36b`)。
  - `CHANGE_REPORT.md` §7:「**獨立審查已進行三輪** … 審查獨立性有已知限制:
    **三輪為同一位審查者** … 複審存在確認偏誤。」
    —— 這個限制**已於第四輪解除**,§6.3 自己就是這麼寫的。
  - `VERIFICATION_REPORT.md:5`「已 commit `99e34d5`」、`:104`「複審已進行**三輪**」——同樣停在上一輪。
- **違反的要求**:與 M-A 完全相同——工作準則的「變更報告已產生,**無未揭露偏差**」
  與「相關文件已同步」。
- **影響**:**方向與 M-A 相反,但落點一樣。** M-A 是把做過的審查寫成沒做;
  這次是把「已有一位獨立審查者複跑證據確認」寫成「三輪都是同一人,有確認偏誤」。
  一個只讀表頭與 §7 決定驗收與否的人,會**低估**這個變更的審查強度,
  並保留一個已經不存在的限制。
  commit 清單停在第一個 SHA 也影響 §8 rollback 的實務判斷(現在要 revert 三個)。
  **危害仍僅限於文件誤導,不涉程式,故與 M-A 同列 Medium,不升級。**
  但值得指出:這是**同一個欄位、同一種矛盾的第三次**——
  §6.2 才剛寫下「第四次過度宣稱,在此收回」,表頭就又落後一輪。
  **真正的問題不是這幾行字,而是「報告表頭沒有納入每輪處置的收尾動作」。**
- **修補方向(有界)**:`CHANGE_REPORT.md` 表頭改「已進行**四輪**;第四輪為**不同**審查者」、
  commit 一行改為不列舉 SHA(理由與 §2 不寫檔案數相同,或寫「見 `git log main..HEAD`」);
  §7 同步改寫並**刪去已解除的確認偏誤限制**(改記本輪第五輪又回到同一人);
  `VERIFICATION_REPORT.md:5/104` 同步。
  **全在 `changes/` 內,無需升 revision。**
  更值得做的是**把「表頭與 §7 同步」寫成每輪處置的固定收尾步驟**——
  這件事已經漏了三次,不是靠下次更小心能解決的。

### Low

**N-3 —— 兩處新寫下的敘述,一寫下就已經不準。**

- **證據/位置**:
  - `IMPLEMENTATION_PLAN.md:159`:「不是單一 SHA——**目前涵蓋 `99e34d5` 與 `62cf5a4`**,
    以及後續審查處置的 commit」——這句話所在的 commit **本身就是 `c1ee36b`**,
    未列其中。後半句「以及後續審查處置的 commit」救得回語意,
    但**前半句的列舉在寫下的那一刻就過期了**——與 §2「7 個檔案」同一種毛病。
  - `TASK_LOG.md` R7〈驗證〉:「本輪只改 **`changes/` 內**的報告文字與 `IMPLEMENTATION_PLAN.md`」
    —— 漏了 **`docs/notes.md`**,而它**不在 `changes/` 內**。
    (`IMPLEMENTATION_PLAN.md` 本來就在 `changes/` 內,這句話的分類本身也是亂的。)
- **影響**:**很小。** 兩處的結論都不受影響:授權語意由後半句涵蓋;
  「不需重跑測試」的結論在 `docs/notes.md` 是 markdown 的前提下仍然成立。
  列為 Low 是因為**它們是 M-B 的同一個根**(敘述沒跟著事實收尾),
  一起修比分開修便宜。
- **修補方向(有界)**:plan 該句刪去 SHA 列舉,只留「本變更的 commit」;
  R7 驗證段補上 `docs/notes.md`。兩行字。

### Suggestion

- **S-E —— 「人類交來審查報告 = 授權繼續」這個推定,值得寫成明文規則。**
  實作者已誠實揭露 R6/R7 沒有依 stop condition 停止,並取得追認。
  但**下一個變更會再遇到同一個情境**:審查報告交來時,實作者要嘛停下報備、
  要嘛就會再一次把「報告被交來」讀成授權。
  這不是這個變更能解決的,建議與 N11 一起放進 skill 層:
  `review-change` 的產出交回實作端時,**應明確附一句「本報告不構成執行授權」**
  (現行 template 只有「The reviewer does not approve...」,是對審查者說的,不是對實作者說的)。
  已有 `docs/notes.md` N10/N11 兩條在等,這條可併入 N11。
- **S-F —— 第五輪(本輪)又回到同一位審查者。**
  第四輪剛解除的獨立性限制,本輪重新出現(見〈Independence disclosure〉)。
  若這個變更要當履歷展示的治理範例,**第四輪的獨立複核已經足夠**,
  不必為了 M-B 這種文件級發現再開一輪外部審查;
  但 §7 的措辭應如實記成「第四輪為獨立審查者,第五輪回到該審查者本人」。

## Requirement and Test Coverage Gaps

- **AC1–AC9 在 `c1ee36b` 上全部成立**,與第四輪相同(本輪零程式碼變更)。
- **測試未重跑**,理由如上;第四輪由我實跑的 `1 failed, 241 passed`(唯一失敗為非乾淨
  volume 的既有 flake)仍是現行證據。
- **守衛的覆蓋邊界現在有明文記載**(§6 L4 (b)),我重測吻合。
  **這是本變更四輪來第一次,「守衛守到哪為止」是寫下來的而不是留白的。**
- **無新增覆蓋缺口。**

## Compatibility, Security, and Scope Assessment

- **契約**:本輪**零契約變更**,commit 分類 `docs(change):` 正確。
- **安全**:不變。無程式碼變更,無新寫入路徑。
- **範圍**:5 個檔案全在 rev 2 批准路徑內;群組路徑本輪一行未動,AC5 持續成立。
- **授權**:R6/R7 的 Task 授權缺口已由人類追認並**同時保留原始事實**
  (待你確認那句回覆屬實,見 N-1)。**push 仍未授權,三個 commit 都只在本機。**
- **Rollback**:無 DB 變更。`git revert c1ee36b 62cf5a4 99e34d5` 完全回復,成本為零。

## Unreviewed Areas and Residual Risk

- ~~「jett 回覆『兩者都在授權內』」無法由我驗證~~ —— **已於本輪由人類直接確認,不再是殘餘風險**。
- **本輪未重跑測試與 lint**(零程式碼變更;lint 仍無容器入口,見已記入的 N10)。
- **未在乾淨 volume 上驗證**(自第一輪未變)。
- **未做人眼前端驗證**(自第一輪未變)。
- **群組路徑本身未複驗**——它是進入圖譜的唯一路徑,品質由先前變更的審查背書。
- **本輪獨立性受限**(與第四輪同一審查者,見上)。

## Human Disposition Required

1. **~~N-1 的他證需要你一句話確認~~ —— 已完成。**
   jett 於 2026-08-13 在本審查 session 直接確認「**我有授權**」。
   **N-1 關閉,R6/R7 的 Task 授權缺口與三個 commit 的授權疑慮一併解除。**
   (實作端可在 `IMPLEMENTATION_PLAN.md` 的授權證據欄補記這一筆:
   人類亦曾直接向第五輪審查者確認,非僅經實作者轉述。)
2. **M-B(Medium,文件)**:表頭與 §7 停在第四輪之前,與同一份文件的 §6.3 矛盾。
   建議修完再驗收(成本兩三行),**並把「表頭同步」變成每輪處置的固定收尾**——
   它已經漏了三次。
3. **N-3(Low)**:兩行字,與 M-B 一起修。
4. **程式面:第四輪起無新發現,可驗收。** `99e34d5` 的六項、`62cf5a4` 的守衛強化
   都經過獨立複跑;`c1ee36b` 零程式碼變更。
5. **R6/R7 未依 stop condition 停止一事,已揭露並追認**——
   請確認你接受這個追認(而非只是被告知)。
6. **push 仍未授權;N7 仍未完成**,三份文件與三則 commit message 都正確揭露。

**本輪核心結論:N-1 / N-2 都真的處置了,N-2 我重測逐行吻合,
而實作者在 N-1 上主動揭露了一件我沒問到、比原發現更嚴重的事(R6/R7 未依 stop condition 停止),
並拒絕用事後追認抹掉事實——這是四輪來品質最高的一次回應。
剩下的 M-B / N-3 都不在程式裡,而是同一件事的第三次:報告表頭沒有跟著處置一起收尾。**

The reviewer does not approve, fix, merge, or release this change.
